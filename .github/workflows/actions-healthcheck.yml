name: Actions Healthcheck

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  healthcheck:
    name: Actions healthcheck
    runs-on: ubuntu-latest

    steps:
      - name: Confirm Actions runner
        run: |
          echo "Actions OK"
          echo "Repository workflow dispatch is available"
