# BLINGLOGS

Pasta reservada para trilhas de diagnóstico do sistema.

Arquivos esperados quando a persistência estiver ativa:

- runtime/latest.jsonl
- runtime/blinglogs_loop.jsonl
- runtime/sessions/*.jsonl

Os registros devem ser sanitizados antes de persistir aqui.
